namespace IconLibrary
{
    [IconEnum(nameof(IconLibrary))]
    public enum EditorIcon
    {
        Abc,
        Close,
        Help,
        Open,
        Import,
        Save,
        Screenshot,
        Refresh,
        Page
    }
}